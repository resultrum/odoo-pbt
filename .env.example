# Odoo Template - Environment Configuration
# Copy this file to .env (git will ignore it)
# ⚠️  NEVER commit .env with real credentials!

# ============================================================================
# DEVELOPMENT: Use defaults below - nothing to change!
# PRODUCTION: See comments for what to override
# ============================================================================

# Database
DB_HOST=db
DB_PORT=5432
DB_NAME=odoo
DB_USER=odoo
DB_PASSWORD=odoo

# PostgreSQL root password
POSTGRES_PASSWORD=odoo

# Odoo
ODOO_ADMIN_PASSWORD=admin

# ============================================================================
# Optional: Only change these if needed
# ============================================================================

# Project identification (used by setup-new-project.sh)
PROJECT_NAME=odoo-template
MODULE_NAME=mta_base
ORG_NAME=resultrum

# Ports (change if conflicts)
ODOO_PORT=8069
ODOO_LONGPOLLING_PORT=8072

# Development mode (set to 0 for production)
ODOO_DEV_MODE=1
